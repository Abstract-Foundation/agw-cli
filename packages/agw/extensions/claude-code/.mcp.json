{
  "mcpServers": {
    "agw": {
      "type": "stdio",
      "command": "agw",
      "args": ["mcp", "serve", "--sanitize", "strict"],
      "env": {
        "AGW_SANITIZE_PROFILE": "strict"
      }
    }
  }
}
